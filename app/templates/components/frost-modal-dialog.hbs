{{#frost-modal
  modalClass='frost-modal-dialog'
  modalName=modalName
  onOpen=(action 'modalOpen')
  onClose=(action 'modalClose')
  confirm=(action 'confirm') as |slot|
}}
  {{#block-slot slot 'target'}}
    {{#yield-slot 'target'}}
      {{yield}}
    {{/yield-slot}}
  {{/block-slot}}

  {{#block-slot slot 'header'}}
    {{frost-icon
      icon=dialogIcon
      class='icon'}}
    <div class='title'>{{title}}</div>
  {{/block-slot}}

  {{#block-slot slot 'body'}}
    {{#yield-slot 'body'}}
      <div class="body">
        {{#if messageTemplate}}
          {{partial messageTemplate}}
        {{else}}
          <span>{{yield}}</span>
        {{/if}}
      </div>
    {{/yield-slot}}
  {{/block-slot}}

  {{#block-slot slot 'footer' as |controls|}}
    {{#if confirmAlias}}
      {{controls.cancel
          autofocus=true
          size='medium'
          priority='tertiary'
          text=cancelAlias
        }}
      {{controls.confirm
          size='medium'
          priority='primary'
          text=confirmAlias}}
    {{else}}
      {{controls.cancel
          autofocus=true
          size='medium'
          priority='primary'
          text=cancelAlias}}
    {{/if}}
    {{/block-slot}}
{{/frost-modal}}
